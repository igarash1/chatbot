{
  "name": "chatbot-example",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Read-only FAQ + order-lookup support chatbot (Gemini + Function Calling)",
  "scripts": {
    "chat": "tsx src/cli.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@google/genai": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.9"
  }
}
